package com.tree.question;

/**
 * https://www.geeksforgeeks.org/dsa/print-nodes-top-view-binary-tree/
 *
 * <ul>Given the root of a binary tree, find the top view of the tree.</ul>
 * <ul>The top view of a binary tree represents the set of nodes visible when the tree is viewed from above.</ul>
 *
 * <ul>Each node in the tree has a horizontal distance (HD) from the root, which helps determine its position when viewed from the top:</ul>
 *
 * <ul>The root node has an HD of 0.</ul>
 * <ul>The left child of a node has an HD equal to parent’s HD - 1.</ul>
 * <ul>The right child of a node has an HD equal to parent’s HD + 1.</ul>
 * Note:
 *
 * <ul>The result should contain the nodes visible from leftmost to rightmost and if two nodes have the same horizontal distance, only the uppermost (first encountered) node should be considered in the top view.</ul>
 */
public class TopView {

}
